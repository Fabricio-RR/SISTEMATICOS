# URL del backend (FastAPI). Copiar este archivo a .env.local:
#   cp .env.local.example .env.local
# Si no se define, el frontend usa http://localhost:8000 por defecto.
NEXT_PUBLIC_API_URL=http://localhost:8000
